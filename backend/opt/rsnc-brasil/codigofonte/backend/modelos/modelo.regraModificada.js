class RegraModificada{
    constructor(regra){
        this.valor = regra.valor;
        this.nome = regra.nome;
        this.id = regra.id;
        this.id_evento = regra.id_evento;
        this.nomeProva = regra.nomeProva;
    }
}

module.exports = RegraModificada;