const fs=require('fs');const path=require('path');function walkDir(dir){fs.readdirSync(dir).forEach(f=>{let p=path.join(dir,f);if(fs.statSync(p).isDirectory())walkDir(p);else if(p.endsWith('.jsx')){let c=fs.readFileSync(p,'utf8');let newC=c.replace(/'\/api\/([^']+)'/g,'\${import.meta.env.VITE_API_URL || \'\'}/api/\');newC=newC.replace(/\\/api\/([^\]+)\/g,'\${import.meta.env.VITE_API_URL || \'\'}/api/\');if(newC!==c){fs.writeFileSync(p,newC);console.log('Fixed',p);}}});}walkDir('frontend_new/src/components');
